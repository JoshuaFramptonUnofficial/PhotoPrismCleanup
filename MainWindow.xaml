<Window x:Class="PhotoPrismCleanup.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PhotoPrism Cleanup"
        Height="650" Width="900"
        Background="{DynamicResource WindowBackground}"
        Foreground="{DynamicResource ControlForeground}"
        PreviewKeyDown="Window_PreviewKeyDown">

    <Window.Resources>
        <!-- Easing for switch animation -->
        <CubicEase x:Key="SwitchEase" EasingMode="EaseOut"/>

        <!-- Sun/Moon Toggle style -->
        <Style x:Key="SwitchToggleStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="60"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid>
                            <Border x:Name="Track" CornerRadius="15" Background="LightGray"/>
                            <Border x:Name="Thumb"
                      Width="26" Height="26" CornerRadius="13"
                      Background="White" HorizontalAlignment="Left"
                      RenderTransformOrigin="0.5,0.5">
                                <Border.RenderTransform>
                                    <TranslateTransform x:Name="ThumbTransform"/>
                                </Border.RenderTransform>
                                <ContentPresenter x:Name="IconPresenter"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="#0047AB"/>
                                <Setter TargetName="IconPresenter" Property="Content" Value="🌜"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ThumbTransform"
                                       Storyboard.TargetProperty="X"
                                       To="30" Duration="0:0:0.3"
                                       EasingFunction="{StaticResource SwitchEase}"/>
                                            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="ThumbTransform"
                                                     Storyboard.TargetProperty="X"
                                                     BeginTime="0:0:0.3">
                                                <SplineDoubleKeyFrame KeyTime="0:0:0"  Value="33"/>
                                                <SplineDoubleKeyFrame KeyTime="0:0:0.05" Value="27"/>
                                                <SplineDoubleKeyFrame KeyTime="0:0:0.10" Value="30"/>
                                            </DoubleAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter TargetName="Track" Property="Background" Value="#FFA500"/>
                                <Setter TargetName="IconPresenter" Property="Content" Value="🌞"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ThumbTransform"
                                       Storyboard.TargetProperty="X"
                                       To="0" Duration="0:0:0.3"
                                       EasingFunction="{StaticResource SwitchEase}"/>
                                            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="ThumbTransform"
                                                     Storyboard.TargetProperty="X"
                                                     BeginTime="0:0:0.3">
                                                <SplineDoubleKeyFrame KeyTime="0:0:0"   Value="-3"/>
                                                <SplineDoubleKeyFrame KeyTime="0:0:0.05" Value="3"/>
                                                <SplineDoubleKeyFrame KeyTime="0:0:0.10" Value="0"/>
                                            </DoubleAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Delete / Keep button styles -->
        <Style x:Key="DeleteBtn" TargetType="Button">
            <Setter Property="Background" Value="Red"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style x:Key="KeepBtn" TargetType="Button">
            <Setter Property="Background" Value="Green"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <DockPanel>
        <!-- Top toolbar -->
        <StackPanel DockPanel.Dock="Top"
                Orientation="Horizontal"
                Background="{DynamicResource ControlBackground}"
                Height="32">
            <Button x:Name="HelpBtn" Content="Help" Width="60" Margin="5,4"
              Click="HelpBtn_Click"/>
            <ToggleButton x:Name="ThemeToggle"
                    Style="{StaticResource SwitchToggleStyle}"
                    Margin="5,4"
                    Checked="ThemeToggle_Checked"
                    Unchecked="ThemeToggle_Unchecked"/>
        </StackPanel>

        <!-- Status bar -->
        <StatusBar DockPanel.Dock="Bottom"
               Background="{DynamicResource ControlBackground}">
            <TextBlock x:Name="StatusText" Text="Not connected"/>
        </StatusBar>

        <!-- Main content -->
        <Grid>
            <!-- Connect panel -->
            <Border x:Name="ConnectGrid"
              Margin="50,30"
              Padding="20"
              CornerRadius="8"
              Background="{DynamicResource ControlBackground}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Host:" Grid.Row="0" Grid.Column="0" Margin="0,4"/>
                    <TextBox x:Name="HostBox" Grid.Row="0" Grid.Column="1" Margin="5"/>

                    <TextBlock Text="Port:" Grid.Row="1" Grid.Column="0" Margin="0,4"/>
                    <TextBox x:Name="PortBox" Grid.Row="1" Grid.Column="1" Margin="5"/>

                    <TextBlock Text="Username:" Grid.Row="2" Grid.Column="0" Margin="0,4"/>
                    <TextBox x:Name="UserBox" Grid.Row="2" Grid.Column="1" Margin="5"/>

                    <CheckBox x:Name="UseKeyBox" Content="Use Key Auth"
                    Grid.Row="3" Grid.Column="1" Margin="5"/>

                    <TextBlock Text="Key Path:" Grid.Row="4" Grid.Column="0" Margin="0,4"/>
                    <TextBox x:Name="KeyBox" Grid.Row="4" Grid.Column="1" Margin="5"/>

                    <TextBlock Text="Password:" Grid.Row="5" Grid.Column="0" Margin="0,4"/>
                    <PasswordBox x:Name="PwdBox" Grid.Row="5" Grid.Column="1" Margin="5"/>

                    <Button x:Name="ConnectBtn" Content="Connect"
                  Grid.Row="6" Grid.ColumnSpan="2"
                  Width="120" Margin="0,10,0,0"
                  HorizontalAlignment="Center"
                  IsDefault="True"
                  Click="ConnectBtn_Click"/>
                </Grid>
            </Border>

            <!-- Review & Settings tabs -->
            <TabControl x:Name="MainTabs"
                  Visibility="Collapsed"
                  Background="{DynamicResource ControlBackground}">
                <!-- Review tab -->
                <TabItem Header="Review">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0"
                    Margin="10"
                    BorderBrush="Gray"
                    BorderThickness="1">
                            <Grid>
                                <Viewbox>
                                    <Grid>
                                        <Image x:Name="PhotoImg"
                           Stretch="Uniform"
                           Visibility="Collapsed"/>
                                        <MediaElement x:Name="VideoPlayer"
                                  LoadedBehavior="Manual"
                                  UnloadedBehavior="Stop"
                                  Visibility="Collapsed"/>
                                    </Grid>
                                </Viewbox>
                                <Grid x:Name="LoadingOverlay"
                      Background="#80000000"
                      Visibility="Collapsed">
                                    <StackPanel HorizontalAlignment="Center"
                              VerticalAlignment="Center">
                                        <ProgressBar IsIndeterminate="True"
                                 Width="200" Height="20"/>
                                        <TextBlock Text="Loading large file…"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               Margin="0,10,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </Border>

                        <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,5">
                            <Button x:Name="DelBtn"
                      Content="Delete"
                      Style="{StaticResource DeleteBtn}"
                      Click="DelBtn_Click"/>
                            <Button x:Name="UndoBtn"
                      Content="Undo (Z/↓)"
                      Click="UndoBtn_Click"/>
                            <Button x:Name="KeepBtn"
                      Content="Keep"
                      Style="{StaticResource KeepBtn}"
                      Click="KeepBtn_Click"/>
                        </StackPanel>

                        <TextBlock Grid.Row="2"
                       x:Name="ProgressText"
                       HorizontalAlignment="Center"
                       Margin="5"/>
                    </Grid>
                </TabItem>

                <!-- Settings tab (with Tools group) -->
                <TabItem Header="Settings">
                    <ScrollViewer>
                        <StackPanel Margin="10">
                            <TextBlock Text="Originals Folder:"/>
                            <TextBox x:Name="FolderBox" Margin="0,0,0,10"/>
                            <TextBlock Text="Thumbnail-Cache Folder:"/>
                            <TextBox x:Name="ThumbCacheBox" Margin="0,0,0,10"/>
                            <TextBlock Text="Show Media Types:" FontWeight="Bold"
                         Margin="0,10,0,4"/>
                            <CheckBox x:Name="ShowPhotosBox" Content="Photos"
                        Margin="0,0,0,4"/>
                            <CheckBox x:Name="ShowVideosBox" Content="Videos"
                        Margin="0,0,0,10"/>

                            <WrapPanel HorizontalAlignment="Right"
                        Margin="0,20,0,0">
                                <Button Content="Save Progress"
                        Width="110" Margin="0,0,5,0"
                        Click="SaveProgress_Click"/>
                                <Button Content="Save Settings"
                        Width="110"
                        Click="SaveSettings_Click"/>
                            </WrapPanel>

                            <!-- Tools group -->
                            <GroupBox Header="Tools" Margin="0,20,0,0">
                                <StackPanel>
                                    <Button Content="Bulk Delete Now"
                          Click="BulkDeleteNow_Click"
                          Margin="0,0,0,5"/>
                                    <Button Content="Clear Thumbnail Cache"
                          Click="ClearCache_Click"
                          Margin="0,0,0,5"/>
                                    <Button Content="Import Photos…"
                          Click="ImportPhotos_Click"/>
                                </StackPanel>
                            </GroupBox>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
            </TabControl>
        </Grid>
    </DockPanel>
</Window>
